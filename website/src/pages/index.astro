---
import { Code } from 'astro:components';
import 'crd-ui/styles.css';
import '../styles/site.css';
import FrameworkSelect from '../components/FrameworkSelect.astro';
import TOC from '../components/TOC.astro';
import CreatedBy from '../components/CreatedBy.astro';
import BrandGrid from '../components/BrandGrid.astro';
import { HeroCard } from '../components/HeroCard';
import { Playground } from '../components/Playground';
import { localization, logos, theming, usage } from '../lib/snippets';

const themes = { light: 'github-light', dark: 'github-dark' };
---

<!doctype html>
<html lang="en" data-fw="react">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Inter:wght@400..700&display=swap"
      rel="stylesheet"
    />
    <title>crd-ui — credit/debit card visualization</title>
    <meta
      name="description"
      content="Framework-agnostic credit/debit card visualization for payment forms. Zero dependencies, themeable, with React, Vue and Svelte support."
    />
    <script is:inline>
      try {
        var fw = localStorage.getItem('crd-ui-fw');
        if (['react', 'vanilla', 'vue', 'svelte'].indexOf(fw) !== -1)
          document.documentElement.dataset.fw = fw;
      } catch (e) {}
    </script>
  </head>
  <body>
    <div class="hero-beams" aria-hidden="true">
      <div class="beam beam-1"></div>
      <div class="beam beam-2"></div>
      <div class="beam beam-3"></div>
    </div>

    <div class="container masthead">
      <h1>crd-ui</h1>
      <span class="for">for</span>
      <FrameworkSelect />
    </div>

    <TOC />

    <main>
      <div class="hero container">
        <a class="announce" href="#playground">
          <span class="announce__text">Introducing card variants</span>
          <span class="announce__divider"></span>
          <span class="announce__arrows">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M5 12h14M13 6l6 6-6 6"/></svg>
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M5 12h14M13 6l6 6-6 6"/></svg>
          </span>
        </a>
        <div class="hero-card"><HeroCard client:load /></div>
        <p class="tagline">
          A credit &amp; debit card component. Dependency-free. Themeable. Localizable.
        </p>
        <div class="hero-actions">
          <button class="btn btn-primary" id="copy-install" type="button">
            <span class="cmd">npm i crd-ui</span>
            <svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
              <rect x="9" y="9" width="12" height="12" rx="2"></rect>
              <path d="M5 15V5a2 2 0 0 1 2-2h10"></path>
            </svg>
          </button>
          <a class="btn btn-secondary" href="https://github.com/JuandaGarcia/crd-ui" target="_blank" rel="noopener">
            GitHub
          </a>
        </div>
      </div>

      <div class="container prose">
        <section id="playground">
          <h2>Try it</h2>
          <p>Type below — brand detection, formatting and the CVC flip are all built in.</p>
          <Playground client:load />
        </section>

        <section id="usage">
          <h2>Usage</h2>
          <p>
            One package for every framework. The core is vanilla; adapters are subpath imports.
          </p>
          <div class="fw-block" data-block="react">
            <Code code={usage.react} lang="tsx" themes={themes} />
          </div>
          <div class="fw-block" data-block="vanilla">
            <Code code={usage.vanilla} lang="js" themes={themes} />
          </div>
          <div class="fw-block" data-block="vue">
            <Code code={usage.vue} lang="vue" themes={themes} />
          </div>
          <div class="fw-block" data-block="svelte">
            <Code code={usage.svelte} lang="svelte" themes={themes} />
          </div>
        </section>

        <section id="brands">
          <h2>Brands</h2>
          <p>
            Live detection for 10 brands, each with its own theme. This grid is rendered by the
            vanilla <code>createCard</code> API.
          </p>
          <BrandGrid />
        </section>

        <section id="theming">
          <h2>Theming</h2>
          <p>Override CSS custom properties on <code>.crd</code> or any ancestor.</p>
          <Code code={theming} lang="css" themes={themes} />
          <p>
            The built-in brand marks are deliberately generic so the package ships no trademarked
            assets — pass your own SVGs if you're licensed to use the official ones:
          </p>
          <div class="fw-block" data-block="react">
            <Code code={logos.react} lang="tsx" themes={themes} />
          </div>
          <div class="fw-block" data-block="vanilla">
            <Code code={logos.vanilla} lang="js" themes={themes} />
          </div>
          <div class="fw-block" data-block="vue">
            <Code code={logos.vue} lang="vue" themes={themes} />
          </div>
          <div class="fw-block" data-block="svelte">
            <Code code={logos.svelte} lang="svelte" themes={themes} />
          </div>
        </section>

        <section id="localization">
          <h2>Localization</h2>
          <p>Every label and placeholder on the card is configurable.</p>
          <div class="fw-block" data-block="react">
            <Code code={localization.react} lang="tsx" themes={themes} />
          </div>
          <div class="fw-block" data-block="vanilla">
            <Code code={localization.vanilla} lang="js" themes={themes} />
          </div>
          <div class="fw-block" data-block="vue">
            <Code code={localization.vue} lang="vue" themes={themes} />
          </div>
          <div class="fw-block" data-block="svelte">
            <Code code={localization.svelte} lang="svelte" themes={themes} />
          </div>
        </section>

        <section id="api">
          <h2>API</h2>
          <p>
            Props of <code>&lt;Card /&gt;</code> — the vanilla <code>createCard(container, options)</code> accepts the same fields and returns
            <code>&#123; update, brand, element, destroy &#125;</code>.
          </p>
          <div class="api-scroll">
            <table class="api-table">
              <thead>
                <tr><th>Prop</th><th>Type</th><th>Description</th></tr>
              </thead>
              <tbody>
                <tr>
                  <td><code>number</code></td><td><code>string</code></td>
                  <td>Card number, formatted and masked per brand as you type.</td>
                </tr>
                <tr>
                  <td><code>name</code></td><td><code>string</code></td>
                  <td>Cardholder name; shows a placeholder while empty.</td>
                </tr>
                <tr>
                  <td><code>expiry</code></td><td><code>string</code></td>
                  <td>Expiry date, normalized to <code>MM/YY</code>.</td>
                </tr>
                <tr>
                  <td><code>cvc</code></td><td><code>string</code></td>
                  <td>Security code, shown on the back.</td>
                </tr>
                <tr>
                  <td><code>variant</code></td>
                  <td><code>'sunset' | 'ember' | 'holo' | 'porcelain' | 'graphite' | 'gradient'</code></td>
                  <td>Card finish; <code>'sunset'</code> (default) tints its bloom to the brand.</td>
                </tr>
                <tr>
                  <td><code>focused</code></td>
                  <td><code>'number' | 'name' | 'expiry' | 'cvc' | null</code></td>
                  <td>Highlights the section; <code>'cvc'</code> flips the card.</td>
                </tr>
                <tr>
                  <td><code>placeholders</code></td><td><code>&#123; name?: string &#125;</code></td>
                  <td>Placeholder text for the empty name.</td>
                </tr>
                <tr>
                  <td><code>locale</code></td><td><code>&#123; validThru?: string &#125;</code></td>
                  <td>Label next to the expiry date.</td>
                </tr>
                <tr>
                  <td><code>logos</code></td><td><code>Partial&lt;Record&lt;Brand, string&gt;&gt;</code></td>
                  <td>Custom inline-SVG brand marks.</td>
                </tr>
                <tr>
                  <td><code>onBrandChange</code></td><td><code>(brand: Brand | null) =&gt; void</code></td>
                  <td>Fires when the detected brand changes (React/Svelte callback; Vue emits <code>@brand-change</code>).</td>
                </tr>
              </tbody>
            </table>
          </div>
        </section>

        <footer class="site-footer">
          <CreatedBy />
        </footer>
      </div>
    </main>

    <div class="dock-mask" aria-hidden="true"></div>
    <nav class="dock" aria-label="Site">
      <div class="dock-inner">
        <a class="dock-item" href="#usage">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" aria-hidden="true">
            <path d="M4 19.5v-15A2.5 2.5 0 0 1 6.5 2H20v20H6.5a2.5 2.5 0 0 1 0-5H20"></path>
          </svg>
          Docs
        </a>
        <a class="dock-item" href="#playground">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" aria-hidden="true">
            <rect x="2" y="5" width="20" height="14" rx="2"></rect>
            <path d="M2 10h20"></path>
          </svg>
          Playground
        </a>
        <a class="dock-item" href="https://github.com/JuandaGarcia/crd-ui" target="_blank" rel="noopener">
          <svg viewBox="0 0 16 16" fill="currentColor" aria-hidden="true">
            <path d="M8 0C3.58 0 0 3.58 0 8c0 3.54 2.29 6.53 5.47 7.59.4.07.55-.17.55-.38 0-.19-.01-.82-.01-1.49-2.01.37-2.53-.49-2.69-.94-.09-.23-.48-.94-.82-1.13-.28-.15-.68-.52-.01-.53.63-.01 1.08.58 1.23.82.72 1.21 1.87.87 2.33.66.07-.52.28-.87.51-1.07-1.78-.2-3.64-.89-3.64-3.95 0-.87.31-1.59.82-2.15-.08-.2-.36-1.02.08-2.12 0 0 .67-.21 2.2.82.64-.18 1.32-.27 2-.27s1.36.09 2 .27c1.53-1.04 2.2-.82 2.2-.82.44 1.1.16 1.92.08 2.12.51.56.82 1.27.82 2.15 0 3.07-1.87 3.75-3.65 3.95.29.25.54.73.54 1.48 0 1.07-.01 1.93-.01 2.2 0 .21.15.46.55.38A8.01 8.01 0 0 0 16 8c0-4.42-3.58-8-8-8Z"></path>
          </svg>
          GitHub
        </a>
        <a class="dock-item" href="https://www.npmjs.com/package/crd-ui" target="_blank" rel="noopener">
          <svg viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
            <path d="M1.763 0C.786 0 0 .786 0 1.763v20.474C0 23.214.786 24 1.763 24h20.474c.977 0 1.763-.786 1.763-1.763V1.763C24 .786 23.214 0 22.237 0zM5.13 5.323l13.837.019-.009 13.836h-3.464l.01-10.382h-3.456L12.04 19.17H5.113z"></path>
          </svg>
          npm
        </a>
      </div>
    </nav>

    <script>
      const btn = document.getElementById('copy-install')!;
      btn.addEventListener('click', async () => {
        await navigator.clipboard.writeText('npm i crd-ui');
        const label = btn.querySelector('.cmd')!;
        const prev = label.textContent;
        label.textContent = 'copied!';
        setTimeout(() => (label.textContent = prev), 1200);
      });
    </script>
  </body>
</html>
